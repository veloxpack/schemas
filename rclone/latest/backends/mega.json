{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.veloxpack.io/rclone/latest/backends/mega.json",
  "title": "Mega",
  "description": "Mega cloud storage",
  "type": "object",
  "required": ["user", "pass"],
  "properties": {
    "user": {
      "type": "string",
      "title": "Username",
      "description": "User name (email address)",
      "format": "email",
      "examples": ["user@example.com"]
    },
    "pass": {
      "type": "string",
      "title": "Password",
      "description": "Password",
      "format": "password"
    },
    "2fa": {
      "type": "string",
      "title": "Two-Factor Authentication Code",
      "description": "The 2FA code of your MEGA account if the account is set up with one",
      "examples": ["123456"]
    },
    "debug": {
      "type": "boolean",
      "title": "Debug",
      "description": "Output more debug from Mega. If this flag is set (along with -vv) it will print further debugging information from the mega backend",
      "default": false
    },
    "hard_delete": {
      "type": "boolean",
      "title": "Hard Delete",
      "description": "Delete files permanently rather than putting them into the trash. If specified, rclone will permanently delete objects instead of putting them in trash",
      "default": false
    },
    "use_https": {
      "type": "boolean",
      "title": "Use HTTPS",
      "description": "Use HTTPS for transfers. MEGA uses plain text HTTP connections by default. Some ISPs throttle HTTP connections, this causes transfers to become very slow. Enabling this will force MEGA to use HTTPS for all transfers",
      "default": false
    },
    "encoding": {
      "type": "string",
      "title": "Encoding",
      "description": "The encoding for the backend (advanced)",
      "default": "Slash,InvalidUtf8,Dot"
    }
  },
  "additionalProperties": false,
  "examples": [
    {
      "user": "user@example.com",
      "pass": "obscured-password"
    },
    {
      "user": "user@example.com",
      "pass": "obscured-password",
      "2fa": "123456",
      "use_https": true
    }
  ]
}

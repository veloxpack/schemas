{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.veloxpack.io/schemas/core/v1alpha1/mixins/bitrate.json",
  "title": "Bitrate",
  "description": "Represents a bitrate value as a string with optional k/M suffix for CRD compatibility. Valid formats: '500', '500k', '7.5M'. No suffix implies bits per second, 'k' suffix indicates kilobits per second (1000 bits/s), and 'M' suffix indicates megabits per second (1000000 bits/s).",
  "type": "string",
  "pattern": "^[\\d\\.]+(?:[kM])?$",
  "examples": [
    "128k",
    "384k",
    "1.5M",
    "8000"
  ]
}


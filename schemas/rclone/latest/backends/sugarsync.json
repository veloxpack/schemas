{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.veloxpack.io/schemas/rclone/latest/backends/sugarsync.json",
  "title": "SugarSync",
  "description": "SugarSync cloud storage",
  "type": "object",
  "properties": {
    "app_id": {
      "type": "string",
      "title": "App ID",
      "description": "Sugarsync App ID. Leave blank to use rclone's",
      "examples": ["/sc/9068489/215_1736969337"]
    },
    "access_key_id": {
      "type": "string",
      "title": "Access Key ID",
      "description": "Sugarsync Access Key ID. Leave blank to use rclone's"
    },
    "private_access_key": {
      "type": "string",
      "title": "Private Access Key",
      "description": "Sugarsync Private Access Key. Leave blank to use rclone's",
      "format": "password"
    },
    "hard_delete": {
      "type": "boolean",
      "title": "Hard Delete",
      "description": "Permanently delete files if true, otherwise put them in the deleted files",
      "default": false
    },
    "encoding": {
      "type": "string",
      "title": "Encoding",
      "description": "The encoding for the backend (advanced)",
      "default": "Slash,Del,Ctl,InvalidUtf8,Dot"
    }
  },
  "additionalProperties": false,
  "examples": [
    {
      "hard_delete": false
    }
  ],
  "$comment": "SugarSync requires username/password during initial setup. The refresh_token, authorization, user, root_id, and deleted_id fields are auto-configured by rclone"
}

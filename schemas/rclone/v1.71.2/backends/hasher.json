{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.veloxpack.io/schemas/rclone/v1.71.2/backends/hasher.json",
  "title": "Hasher",
  "description": "Better checksums for other remotes (cache MD5/SHA1)",
  "type": "object",
  "properties": {
    "remote": {
      "type": "string",
      "title": "Remote",
      "description": "Remote to cache checksums for (e.g. myRemote:path)"
    },
    "hashes": {
      "type": "array",
      "title": "Hash Types",
      "description": "Comma-separated list of supported checksum types",
      "items": {
        "type": "string",
        "enum": [
          "md5",
          "sha1",
          "sha256",
          "crc32",
          "dropbox",
          "quickxor"
        ]
      },
      "default": [
        "md5",
        "sha1"
      ]
    },
    "max_age": {
      "type": "string",
      "title": "Max Age",
      "description": "Maximum time to keep checksums in cache (0=no cache, off=forever)",
      "default": "off",
      "examples": [
        "1h",
        "24h",
        "off",
        "0"
      ]
    }
  },
  "required": [
    "remote"
  ],
  "additionalProperties": false
}
